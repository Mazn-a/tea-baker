# لوحة الإدارة

- الرابط: `/admin/`
- رمز الدخول من `js/config.js` → `adminPin`
- إعداد قاعدة البيانات: `docs/SETUP-AR.md`

عند الدخول ترى صندوق **حالة الربط**:
- سحابة = جاهز للاستخدام الحقيقي بين الأجهزة
- محلي = تجريبي على نفس المتصفح فقط
